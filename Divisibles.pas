Program Divisibles;
Var
  n1, n2 : Integer;
  divisor : Integer;
Begin
  Writeln('Ingrese el numero deseado');
  Readln(n1);
  Writeln('Ingrese el numero deseado');
  Readln(n2);
  divisor := n2;
  If n1 Mod divisor = 0 Then
    If n1 Mod divisor = 0 Then
      Writeln('es divisible al menos 2 veces')
  Else
    Writeln('no es');
		Readln
End.
